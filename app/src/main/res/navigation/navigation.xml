<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation"
    app:startDestination="@id/locationSearchFragment">
    <fragment
        android:id="@+id/locationSearchFragment"
        android:name="com.michs.weatherapp.locationSearch.LocationSearchFragment"
        android:label="LocationSearchFragment" >
        <action
            android:id="@+id/action_locationSearchFragment_to_currentWeatherFragment"
            app:destination="@id/currentWeatherFragment"/>
    </fragment>
    <fragment
        android:id="@+id/currentWeatherFragment"
        android:name="com.michs.weatherapp.currentWeather.CurrentWeatherFragment"
        android:label="CurrentWeatherFragment">
    </fragment>
</navigation>